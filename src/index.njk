---
permalink: "/"
layout: '00-core/base.njk'
---

{# Home Title Section #}
{% from "01-organisms/section-title.njk" import renderSectionTitle %}

{{ renderSectionTitle({ 
    name: "home_title",
    datas: organisms["section-titles"],
    images: atoms.images
}) }}

{# Home Hero Section with ID for scrolling #}
<div id="home-hero">
{% from "01-organisms/section-hero.njk" import renderSectionHero %}

{{ renderSectionHero({ 
    name: "home_hero",
    datas: organisms["section-heros"],
    headings: atoms.headings,
    buttons: atoms.buttons,
    images: atoms.images,
    links: atoms.links,
    icons: atoms.icons
}) }}
</div>

{# Team Carousel Section #}
{% from "01-organisms/section-carousel.njk" import renderSectionCarousel %}

{{ renderSectionCarousel({ 
    name: "home_team", 
    datas: organisms["section-carousels"], 
    headings: atoms.headings, 
    links: atoms.links,
    icons: atoms.icons,
    carousels: organisms.carousels,
    buttons: atoms.buttons,
    images: atoms.images,
    reversed: true
}) }}

{# Local Carousel Section #}
{% from "01-organisms/section-carousel.njk" import renderSectionCarousel %}

{{ renderSectionCarousel({ 
    name: "home_local", 
    datas: organisms["section-carousels"], 
    headings: atoms.headings, 
    links: atoms.links,
    icons: atoms.icons,
    carousels: organisms.carousels,
    buttons: atoms.buttons,
    images: atoms.images
}) }}

{# Why Hero Section #}
{% from "01-organisms/section-hero.njk" import renderSectionHero %}

{{ renderSectionHero({ 
    name: "home_why",
    datas: organisms["section-heros"],
    headings: atoms.headings,
    buttons: atoms.buttons,
    images: atoms.images,
    links: atoms.links,
    icons: atoms.icons
}) }}

{# Services Hero Section #}
{% from "01-organisms/section-hero.njk" import renderSectionHero %}

{{ renderSectionHero({ 
    name: "home_services",
    datas: organisms["section-heros"],
    headings: atoms.headings,
    buttons: atoms.buttons,
    images: atoms.images,
    links: atoms.links,
    icons: atoms.icons
}) }}

{# Infos Highlight Section with ID for scrolling #}
<div id="home-infos">
{% from "01-organisms/section-highlight.njk" import renderSectionHighlight %}

{{ renderSectionHighlight({ 
    name: "ma_nautic_services",
    datas: organisms["section-highlights"],
    headings: atoms.headings,
    buttons: atoms.buttons,
    images: atoms.images,
    links: atoms.links
}) }}
</div>

{# Activities Carousel Section #}
{% from "01-organisms/section-carousel.njk" import renderSectionCarousel %}

{{ renderSectionCarousel({ 
    name: "home_activities", 
    datas: organisms["section-carousels"], 
    headings: atoms.headings, 
    links: atoms.links,
    icons: atoms.icons,
    carousels: organisms.carousels,
    buttons: atoms.buttons,
    images: atoms.images
}) }}

{# Excursion Hero Section #}
{% from "01-organisms/section-hero.njk" import renderSectionHero %}

{{ renderSectionHero({ 
    name: "home_excursions",
    datas: organisms["section-heros"],
    headings: atoms.headings,
    buttons: atoms.buttons,
    images: atoms.images,
    links: atoms.links,
    icons: atoms.icons
}) }}